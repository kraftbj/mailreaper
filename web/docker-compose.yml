services:
  mailreaper:
    build: .
    ports:
      - "127.0.0.1:8025:8025"
    environment:
      - TZ=${TZ:-UTC}
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - mailreaper-data:/app/data
    restart: unless-stopped

volumes:
  mailreaper-data:
